version: 2
updates:
- package-ecosystem: "pip"
  directory: "/"
  schedule:
    interval: "weekly"
git config --global commit.gpgsign true
version: 2
updates:
- package-ecosystem: "pip"
  directory: "/"
  schedule:
    interval: "weekly"
git config --global commit.gpgsign true
docker run --read-only --memory=512m
JWT
Rate limiting
Input validation
